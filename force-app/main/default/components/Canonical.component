<apex:component controller="CanonicalController">
  <apex:pageBlock title="My Content">
    <apex:pageBlockTable value="{!accounts}" var="item">
        <apex:column value="{!item.id}"/>
        <apex:column value="{!item.name}"/>
    </apex:pageBlockTable>
</apex:pageBlock>
</apex:component>